{
	"name": "socialynx-functions",
	"main": "lib/index.js",
	"scripts": {
		"build": "tsc",
		"serve": "npm run build && firebase emulators:start --only functions",
		"deploy": "npm run build && firebase deploy --only functions"
	},
	"engines": {
		"node": "22"
	},
	"dependencies": {
		"axios": "^1.7.2",
		"firebase-admin": "^11.10.1",
		"firebase-functions": "^4.8.0",
		"openai": "^4.49.2"
	},
	"devDependencies": {
		"@types/node": "^20.11.0",
		"typescript": "^5.4.5"
	}
}
