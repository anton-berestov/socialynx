// TODO: Integrate Yandex Mobile Ads SDK once native modules are configured.
// Placeholder interface to avoid runtime errors in Expo Go.
export const adService = {
  showInterstitial: async () => {
    // Integrate actual SDK call in bare workflow
  },
  loadBanner: async () => {
    // Implement banner loading in a dedicated component
  }
};
